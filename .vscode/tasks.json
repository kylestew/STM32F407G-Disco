{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build all",
            "group": "build",
            "type": "shell",
            "command": "make",
            "args": ["all"],
            "options": {
                "cwd": "${fileDirname}"
            }
        },
        {
            "label": "Build clean",
            "group": "build",
            "type": "shell",
            "command": "make",
            "args": ["clean"],
            "options": {
                "cwd": "${fileDirname}"
            }
        },
        {
            "label": "Flash",
            "group": "build",
            "type": "shell",
            "command": "make",
            "args": ["flash"],
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ]
}
